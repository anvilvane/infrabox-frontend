# Public site origin, used for canonical URLs and Open Graph metadata.
# This repository is the marketing front end only: it has no server routes,
# no authentication and no connection to the Infrabox application.
NEXT_PUBLIC_SITE_URL=
